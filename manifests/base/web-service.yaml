apiVersion: v1
kind: Service
metadata:
  name: sandbox-auth-web
  labels:
    app.kubernetes.io/name: sandbox-auth-web
    app.kubernetes.io/part-of: sandbox-auth
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sandbox-auth-web
  ports:
    - name: http
      port: 3000
      targetPort: http
