apiVersion: v1
kind: Service
metadata:
  name: sandbox-auth-api
  labels:
    app.kubernetes.io/name: sandbox-auth-api
    app.kubernetes.io/part-of: sandbox-auth
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: sandbox-auth-api
  ports:
    - name: http
      port: 8080
      targetPort: http
